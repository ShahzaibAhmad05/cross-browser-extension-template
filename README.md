# cross-browser-extension-template

Vite/ESBuild cross-browser compatible extension starter template. I put it here so I don't have to call the AI to write the boilerplate code each time I build one.
